public class TestLinkedList {
/*TestLinkedList tll;
    // Properties
    Node head;
    int count;
    // Constructors

    public TestLinkedList(Node newHead){
        head=newHead;
        count=1;
    }
    // Methods

    //add

    //get

    //size
    //is Empty
    //remove
    public void add(int newData){
        Node temp= new Node(newData);
        Node current=head;
       // while(current.getNext()!=null){
      //      current=current.getNext();

        }
      //  current.setNext(temp);
      //  count++;
    }

    public int get(int index){
        if (index<=0){
            return -1;
        }
        Node current=head;
        for (int i=1; i<index;i++){
       //     current=current.getNext();
        }
      //  return current.getData();
    }

    public int size(){
        return count;
    }

    public boolean isEmpty(){
        return head==null;
    }


    public static void main(String args[]) {
  //      Node n=null;
    //    TestLinkedList head1 =new TestLinkedList(n);
//Node test= new Node(1);
//int test1=test.getData();
//System.out.println(head1);

        Node n= new Node(100);
        TestLinkedList tll=new TestLinkedList(n);

System.out.println(n.data +" "+ n.next+" GET DATA METHOD: "
     //   + n.getData()
+" Count "+tll.count+" head val in tll: "//+tll.head.getData()
) ;

tll.add(200);
        System.out.println(n.data +" "+ n.next+" GET DATA METHOD: "
               // + n.getData()
                +" Count "+tll.count+" head val: "+tll.head
        ) ;

        tll.add(300);
        System.out.println(n.data +" "+ n.next+" GET DATA METHOD: "
               // + n.getData()
                +" Count "+tll.count+" head val: "+tll.head
        ) ;
    }*/
}
